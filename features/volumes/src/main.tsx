import React from 'react'
import ReactDOM from 'react-dom/client'
import { Volumes } from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Volumes title="Volumes" />
  </React.StrictMode>,
)
